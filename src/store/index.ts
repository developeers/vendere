import { createStore } from 'vuex'

import ProductModule from './modules/Product'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ProductModule
  }
})
